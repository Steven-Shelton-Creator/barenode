# Session Log — 2026-07-12

## Completed

- **CH02 — History** — Full implementation cycle

## What Was Built

### Provider seam refactored
- `src/model/provider.py`: `chat()` signature changed from `message: str` to `messages: list[dict]`
- All four provider implementations (`_call_ollama`, `_call_openrouter`, `_call_lstudio`, `_call_fake`) updated to accept and use the full messages list
- Fake provider now finds the last user message from the list instead of echoing raw input

### Agent with history
- `src/harness/agent.py`: Added `self.messages = []` in `__init__`
- `send()` now appends user message, passes full list to `chat()`, appends reply
- No changes needed in the REPL (`main.py`) — history is internal to the Agent

### Tests
- `tests/test_ch02.py`: 4 new tests covering message growth, full history forwarding, turn order, and isolated instances
- `tests/test_ch01.py`: Updated `test_invalid_model_spec` to pass `list[dict]` instead of `str`
- All 10 tests passing

### Demo updated
- `main.py:demo()` now shows CH02 status and message count

### Documentation
- `docs/phases/02-history.md`: Status → ✅ Complete, learnings filled, verification results recorded
- `BUILD_PLAN.md`: CH02 marked complete
- `ROADMAP.md`: CH01 + CH02 marked complete

## Key Decisions

- **Provider signature change**: Rather than keep `chat(message: str)` and have the Agent assemble messages internally, we changed the provider to accept `list[dict]` directly. This aligns with every LLM API (Ollama, OpenAI, OpenRouter all expect a messages array).

## Next

- **CH03 — Instructions**: System prompt loader + `agents.md` auto-load. The phase doc is already scaffolded.

## Verification

```
$ uv run pytest tests/ -v  →  10 passed (0.03s)
$ BARENODE_MODEL=fake/echo uv run demo  →  6 messages tracked
$ BARENODE_MODEL=ollama/qwen3.5:9b uv run agent
  > My name is Gemma.
  > What is my name?
  Your name is Gemma!    ← remembers across turns
```