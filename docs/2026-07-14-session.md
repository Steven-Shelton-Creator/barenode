# Session Log — 2026-07-14

## Completed

- **Git de-sync diagnosis & fix** — Detached HEAD at `CH01` tag instead of `master` branch
- **Preventive guardrails added** to `AGENTS.md`

## What Was Fixed

### Root Cause
The working tree was checked out on tag `CH01` (detached HEAD) instead of branch `master`. All CH02–CH04 work existed on `master` but was invisible because the working directory reflected the `CH01` snapshot.

### Fix
- `git checkout master` — HEAD now at `9670fa7` (latest, post-CH04)
- Verified all CH02–CH04 code is present and operational

### Preventive Rules Added to `AGENTS.md`
- **Git Discipline** (top of Identity section): Always run `git branch --show-current && git log --oneline -1` before any operation
- **Git Branch Verification** (orientation section): Detailed rules — never develop on tags, always work on `master`, report detached HEAD immediately

## Test Results
```
$ uv run pytest tests/ -v
```
(To be run next session to confirm all tests pass on correct branch.)

## Completed
- **CH05 — Tools** — Full implementation cycle

## What Was Built

### `src/model/provider.py`
- `ModelResponse` dataclass — structured return type (content + tool_calls)
- All providers updated to return `ModelResponse`
- `fake_set_next_tool_calls()` — test helper to simulate tool calls

### `src/harness/tools.py`
- `Tool` dataclass (name, description, parameters, fn, requires_approval, needs_workspace)
- `ToolRegistry` — register, get, specs, execute, requires_approval, needs_workspace
- `_calculator` — safe eval with math module, no builtins
- `_read_file` — workspace-confined file reading
- `_write_file` — workspace-confined file writing, creates dirs
- `default_registry()` — pre-populated with all built-in tools

### `src/harness/approval.py`
- `prompt_approval()` — y/n prompt on stderr, fails safe on rejection

### `src/harness/agent.py`
- Tool loop in `send()` — capped at 6 iterations
- `needs_workspace` injection — only injects workspace for tools that need it
- Approval gate check before executing dangerous tools

### `tests/test_ch05.py`
- 38 tests: Tool dataclass, ToolRegistry, calculator, read_file, write_file, default registry, approval, agent integration, regression

## Test Results
```
$ uv run pytest tests/ -v
============================= 71 passed in 0.05s ==============================
```

## Real Model Verification
```
$ uv run python3 -c "..."
> Calculate 256 * 8
> 256 * 8 is 2048.

> Read the file facts.txt using the read_file tool
> The content of facts.txt is: The sky is blue and the sun is warm.
```

## Completed
- **CH06 — Context Management** — Full implementation cycle

## What Was Built

### `src/harness/limits.py`
- Token budget constants: MAX_CONTEXT_TOKENS (4096), MAX_ITEM_CHARS (4000), HEAD_TURNS (2), TAIL_TURNS (3)
- `estimate_tokens(text)` — heuristic: len(text) // 4
- `estimate_messages_tokens(messages)` — sums content + tool_calls + overhead
- `is_budget_exceeded(messages, budget)` — threshold check

### `src/harness/compaction.py`
- `clamp_content(messages, max_chars)` — truncates over-long items with note, non-mutating
- `compact(messages, budget, head_turns, tail_turns)` — keeps head/tail, compresses middle
- Recursive reduction with floor of 1 for head/tail

### `src/harness/agent.py`
- Wired `compact()` into `send()` — fires after user message, before tool loop
- `BARENODE_CONTEXT_BUDGET` env var for configurable budget

### `tests/test_ch06.py`
- 29 tests: 8 limits, 4 clamp, 7 compact, 5 agent integration, 5 regression

## Test Results
```
$ uv run pytest tests/ -v
============================= 100 passed in 0.07s ==============================
```

## Real Model Verification
```
> My name is Gemma. Remember that.
> What is my name?
> Your name is Gemma.

> Calculate 2 + 2 using the calculator tool.
> The result of 2 + 2 is 4.
```

## Completed
- **CH07 — Skills** — Full implementation cycle

## What Was Built

### `src/harness/skills.py`
- `Skill` class — loads skill.md, parses frontmatter (name, description), stores body
- `parse_frontmatter()` — lightweight YAML-like frontmatter parser
- `load_skills()` — scans skills/ directory, returns list of Skill objects
- `build_skills_section()` — builds formatted skills block for system prompt

### `src/harness/instructions.py`
- `make_system_prompt()` now accepts `skills_dir` — appends skills section
- Progressive disclosure: only name + description in prompt, full body on disk

### `skills/sign-off/skill.md`
- Example skill: "Signs off each response with the project code word hila"

## Test Results
```
126 passed in 0.09s
```

## Real Model Verification
```
> Please sign off for me.
> The task is complete.
>
> hila
```
The code word "hila" lived only in the skill file on disk — never in the prompt.

## Completed
- **CH08 — Sandbox** — Full implementation cycle

## What Was Built

### `src/harness/sandbox.py`
- `Sandbox` class with `check()` (Docker availability) and `run(command, workdir)`
- Docker mode: no network, read-only rootfs, non-root user, memory/process caps
- Local fallback: scrubbed subprocess with restricted environment
- `SandboxResult` dataclass: stdout, stderr, exit_code, duration, method

### `src/harness/tools.py`
- Added `bash` tool — runs shell commands through the sandbox
- Requires approval (dangerous tool)
- Needs workspace (mounted at /workspace in Docker container)

## Test Results
```
141 passed in 2.31s
```

---

## Completed
- **CH09 — Durable State** — Full implementation cycle

## What Was Built

### `src/harness/memory.py`
- `save_session()` — writes JSONL session file
- `load_session()` — reads JSONL, returns messages list
- `list_sessions()` — lists all session files
- `search_sessions()` — keyword search across sessions
- `clear_session()` — deletes session files

### `src/harness/agent.py`
- Added `session_name` and `session_dir` parameters
- Loads session from disk on `__init__`
- Calls `save_session()` after every `send()`

### `tests/conftest.py`
- Added `_isolate_session` fixture — unique temp session dir per test
- Prevents test state leakage (bug fix: sessions accumulated across runs)

## Test Results
```
180 passed in 2.90s
```

## Next Up
- CH10 — Planning: orchestrator, step gates, /plan command

---

## Completed
- **CH10 — Planning** — Full implementation cycle

## What Was Built

### `src/harness/orchestrator.py`
- `Step`, `Plan` dataclasses — structured plan representation
- `generate_plan()` — model breaks task into JSON steps
- `format_plan()` — human-readable plan display
- `execute_plan()` — drives each step through the agent
- `_extract_json()` — robust JSON extraction from model output
- Step approval gate, retry on failure

### `src/main.py`
- `/plan <task>` command in REPL — generates, displays, and executes plans
- `_approve_step()` callback for step-by-step approval
- `uv run plan` demo entry

## Test Results
```
180 passed in 2.90s
```

## Next Up
- CH11 — Subagents: delegate + fan-out

---

## Real Model Verification
```
> echo hello from sandbox
> hello from sandbox
> [sandbox: docker, 0.20s]

> Workspace write through sandbox:
> sandbox wrote this
> File exists: True
```

## Next Up
- CH09 — Durable State: JSONL session persistence

---

## Completed
- **CH11 — Subagents** — Full implementation cycle

## What Was Built

### `src/harness/subagent.py`
- `run_subagent()` — creates a fresh Agent, sends one message, returns text reply
- `delegate(task)` — single subtask delegation with isolated context
- `fan_out(tasks)` — parallel subtasks via ThreadPoolExecutor, ordered results
- Unique session naming (thread-safe counter) to avoid file collisions
- Error isolation — per-subtask try/except, one failure doesn't crash others

### `src/harness/tools.py`
- Registered `delegate` tool — single task, requires approval
- Registered `fan_out` tool — multiple tasks, requires approval
- Lazy imports to avoid circular dependency (tools → subagent → agent → tools)

### `tests/test_ch11.py`
- 16 tests: run_subagent, delegate, fan_out, parallelism, context isolation, error handling, registry integration, regression

## Test Results
```
196 passed in 3.03s
```

## Next Up
- CH12 — Self-Verification: test gate

---

## Completed
- **CH12 — Self-Verification** — Full implementation cycle

## What Was Built

### `src/harness/verify.py`
- `CODE_EXTENSIONS` — set of 50+ file extensions considered "code" (`.py`, `.js`, `.go`, `.rs`, etc.)
- `parse_test_command()` — reads `[testing]` section from AGENTS.md
- `was_code_file_written()` — inspects tool calls for `write_file` with code paths
- `check_test_result()` — parses bash result for `[exit code: N]` marker
- `build_verification_prompt()` — message to ask agent to run the test suite

### `src/harness/agent.py`
- Three-state verification gate: Idle → Armed → Waiting
- Arms on code file write, injects verification prompt on next model call
- Checks bash result for exit code 0 after agent runs tests
- Re-arms if tests fail, clears if tests pass

### `AGENTS.md`
- Expanded `## Testing` section with verification instructions

### `tests/test_ch12.py`
- 28 tests: code extensions, test command parsing, path detection, result checking, agent integration, regression

## Test Results
```
224 passed in 2.52s
```

## Next Up
- CH13 — Observability: OpenTelemetry traces, cost tracking