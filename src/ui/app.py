"""Terminal UI entry point (uv run tui, CH14+)."""

import sys


def run() -> None:
    """Launch the two-pane TUI."""
    print("barenode TUI — coming in CH14")
    sys.exit(0)


if __name__ == "__main__":
    run()