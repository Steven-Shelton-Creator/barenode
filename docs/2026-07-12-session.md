# Session Log — 2026-07-12

## Completed

- **CH02 — History** — Full implementation cycle (tagged)
- **Workflow reflections** — Renamed `workflow-playbook.md` → `workflow-reflections.md`, reframed as post-hoc record
- **ADR-004** — Documented CH02 provider signature decision
- **CH02 verification log** — Created `docs/verification/CH02-verification.md`
- **Daily reflections** — Created `docs/reflections/` category with 2026-07-11.md and 2026-07-12.md
- **Schema map** — Created `docs/schema-map.md` with full directory tree, tracking systems summary, phase status table, start-here guide
- **Schema map expansion** — Added session logs, uv.lock, .gitignore, skills directory, research images, and missing reference docs
- **Intake valve** — Created `scripts/intake.sh` with memory-only credential security (ADR-005)
- **CH03 — Instructions** — Full implementation cycle (tagged) with real model verification

## Full Conversation Summary

### How the Session Started

The previous session had completed CH01 (bare model call, tagged). All remaining chapter files were scaffolded as stubs (placeholder comments). This session began by picking up at CH02.

### Directory Context Check

We were working inside `/home/steven/projects/barenode`. The user explicitly noted this is not the `ghoststack` project — separate directory, separate build. Verified: `barenode` exists alongside `ghoststack` under `/home/steven/projects/`.

### Implementation Flow

1. **Read & Plan:** The phase doc `docs/phases/02-history.md` already had a drafted plan. The transcript said: *"Just a list: `self.messages` in `__init__`. Every turn: append user message, send whole list, append reply. Diff: three lines."*

2. **Decide:** One architectural choice emerged — the provider `chat()` function needed to accept `list[dict]` instead of `str`. Chose to change the signature rather than wrap/hide the transformation. Recorded as ADR-004.

3. **Build:**
   - `src/model/provider.py`: Changed `chat(message: str)` → `chat(messages: list[dict])`. Updated all 4 backends (ollama, openrouter, lstudio, fake). The fake provider now extracts the last user message from the list.
   - `src/harness/agent.py`: Added `self.messages = []`, updated `send()` to append and replay full history.
   - `src/main.py`: Updated demo to show CH02 message count (no functional changes needed for REPL).

4. **Test:** Created `tests/test_ch02.py` with 4 tests (message growth, full forwarding, turn order, isolated instances). Updated `test_ch01.py` for new signature. All 10 tests passed.

5. **Verify:**
   - Fake provider demo: 6 messages tracked across 3 turns
   - Real model (ollama/qwen3.5:9b): "My name is Gemma" → "What is my name?" → "Your name is Gemma!" ✓

### Surprises / Learnings

- The phase doc initially underestimated scope (said only `agent.py` changes). The provider seam needed a full signature change across 4 implementations.
- The real work was in `provider.py`, not `agent.py`. The Agent change was truly 3 lines.
- The REPL needed zero changes — all history is internal to the Agent.
- The workflow-playbook was actually written *after* CH01 as a reflection, not a prescriptive guide. Renamed to `workflow-reflections.md` to make this clear.

## What Was Built

### Provider seam refactored
- `src/model/provider.py`: `chat()` signature changed from `message: str` to `messages: list[dict]`
- All four provider implementations updated
- Fake provider now extracts last user message from list

### Agent with history
- `src/harness/agent.py`: `self.messages = []`, `send()` appends + replays

### Tests
- `tests/test_ch02.py`: 4 new tests (all passing)
- `tests/test_ch01.py`: Updated for new signature

### Demo updated
- `main.py:demo()` now shows CH02 status

### Documentation
- `docs/phases/02-history.md`: Complete with conversation log section
- `docs/workflow-reflections.md`: Renamed, reframed as post-hoc reflection
- `DECISIONS.md`: ADR-004 added
- `docs/2026-07-12-session.md`: This file — full conversation log
- `BUILD_PLAN.md`, `ROADMAP.md`: Status updated

## Key Decisions

| Decision | Choice | Rationale |
|----------|--------|-----------|
| Chat signature | `chat(messages: list[dict])` | Matches every LLM API natively. No hidden wrapping. |
| Workflow doc framing | Post-hoc reflection | Created after the fact to observe our own process, not prescribe it. |
| Credential security | Unexpanded `$GITHUB_TOKEN` in `.git/config` | Token value never touches disk. Only the literal string `$GITHUB_TOKEN` stored — inert without env var. See ADR-005. |

## Git Tags

- `CH01` — existed
- `CH02` — created (befed26)

## Commits (after CH02)

| Commit | Description |
|--------|-------------|
| `c96a0db` | Rename workflow-playbook → workflow-reflections, ADR-004, conversation summary |
| `1203984` | Fix context tracking — verification log, schema map, daily reflections |
| `94cdd29` | Expand schema map — tracking systems, phase status, start-here guide |

## Next

- **CH03 — Instructions**: System prompt + `agents.md` auto-load + workspace confinement. Phase doc already scaffolded.

## Verification

```
$ uv run pytest tests/ -v  →  10 passed (0.03s)
$ BARENODE_MODEL=fake/echo uv run demo  →  6 messages tracked
$ BARENODE_MODEL=ollama/qwen3.5:9b uv run agent
  > My name is Gemma.
  > What is my name?
  Your name is Gemma!    ← remembers across turns
```